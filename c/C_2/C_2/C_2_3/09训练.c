#include <stdio.h>
int main09()
{
	//分析下面代码的执行结构，并通过调试器进行验证
	           // {{0,1},{1,2},{4,5)}}
	int a[3][2] = {(0,1),(1,2),(4,5)};// 逗号表达式
				//{ 1 ,2,5,0,0,0}
	int *p;
	p=a[1];//a[1]是空间 理解为 省略 +0    a[1]+0
	printf("%d\n",p[0]);//p[0] 拆  *(p+ 0)   *p

	
	return 0;
}