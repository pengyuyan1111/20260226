#define _CRT_SECURE_NO_WARNINGS  //关闭安全警告 务必放第一句
#include<stdio.h>

int mainsssssss()
{
	////int a=1, b=2, c=3;
	////建议的输入格式( 不加任何修饰)
	////scanf ("%d%d%d", &a, &b, &c);
	////scanf("%d,%d,%d", &a, &b, &c);
	///*scanf("a=%d,b=%d,c=%d", &a, &b, &c);
	//printf("%d %d %d\n",a,b,c); */
	////小数的输入
	//float m = 0;//float 单精度
	//double n=0;//double 双精度

	////scanf("%f%f", &m,&n);
	//scanf("%f",&m);
	//scanf("%lf", &n);
	//printf("%.2f %.2f\n",m,n);
	int age=5;
	{
		double age=1.5;
	}
	   
	return 0;
}